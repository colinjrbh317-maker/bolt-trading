---
import Landing from "../components/Landing.astro";
import { siteConfig } from "../data/config";

const price = siteConfig.price;
const stripeLink = import.meta.env.STRIPE_LINK_A || siteConfig.stripeUrl;
---

<Landing price={price} stripeLink={stripeLink} variant="a" />

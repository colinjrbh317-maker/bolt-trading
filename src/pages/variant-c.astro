---
import Landing from "../components/Landing.astro";
import { siteConfig } from "../data/config";

const price = 40;
const stripeLink = import.meta.env.STRIPE_LINK_C || siteConfig.stripeUrl;
---

<Landing price={price} stripeLink={stripeLink} variant="c" />
